<%= form_for @comment, url: '#' do |f| %>
  <div class="form-group">
    <%= f.text_area :content, class: 'form-control', placeholder: 'Enter your content here' %>
  </div>

  <%= f.submit 'Post Comment', class: 'btn btn-primary' %>
<% end %>